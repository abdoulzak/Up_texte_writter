import { NumberIntMaskDirective } from './number-int-mask.directive';

describe('NumberIntMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberIntMaskDirective();
    expect(directive).toBeTruthy();
  });
});
